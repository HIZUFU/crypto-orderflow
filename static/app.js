const $ = (id) => document.getElementById(id);
const fmt = (value, digits = 2) => value == null || Number.isNaN(Number(value)) ? "-" : Number(value).toFixed(digits);
const pct = (value, digits = 1) => `${(Number(value || 0) * 100).toFixed(digits)}%`;
const esc = (value) => String(value ?? "").replace(/[&<>\"']/g, (c) => ({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[c]));
const score = (value) => { const number = Number(value); return Number.isFinite(number) && number >= 0 && number <= 1 ? `${Math.round(number * 100)} / 100` : `legacy ${fmt(value, 2)}`; };
let lastAlertIds = new Set();

async function api(path, options = {}) { const response = await fetch(path, options); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.detail || "Request failed"); return body; }
function toast(message, kind = "info") { const node = $("toast"); node.textContent = message; node.className = `toast visible ${kind}`; clearTimeout(window.toastTimer); window.toastTimer = setTimeout(() => { node.className = "toast"; }, 3500); }
function sideClass(direction) { return direction === "LONG" ? "long" : "short"; }
function sourceLabel(item) { return `${item.exchange || "-"} · ${item.market || "-"}`; }
function renderSources(sources) { const values = Object.values(sources || {}); $("exchange-badge").textContent = values.length ? `${values.length} markets` : "No markets"; $("source-popover").innerHTML = `<div class="source-popover-head"><span>Active markets</span><a href="/settings.html">Manage</a></div>${values.length ? values.map((source) => `<a href="/chart.html?source_key=${encodeURIComponent(source.source_key)}"><span><b>${esc(source.symbol)}</b> · ${esc(source.exchange)} ${esc(source.market)}</span><small>${esc(source.label)}</small></a>`).join("") : `<div class="empty">No enabled connection.</div>`}`; }
function bindSourceMenu() { $("source-trigger").addEventListener("click", () => { const open = $("source-popover").classList.toggle("hidden"); $("source-trigger").setAttribute("aria-expanded", String(!open)); }); document.addEventListener("click", (event) => { if (!event.target.closest(".source-menu")) $("source-popover").classList.add("hidden"); }); }
function renderSummary(health, stats, analytics) { const sourceCount = Object.keys(health.sources || {}).length; const readyCount = Object.values(health.books_ready || {}).filter(Boolean).length; $("summary").innerHTML = [["Market streams", health.stream_connected ? "Live" : "Offline", health.stream_connected ? "positive" : "negative", `${readyCount}/${sourceCount} order books ready`], ["Candidates", analytics.total_alerts, "", `${pct(analytics.conversion_rate)} opened as paper`], ["Paper equity", `${fmt(stats.equity)} USDT`, stats.equity >= stats.initial_balance ? "positive" : "negative", `realized ${fmt(stats.realized_pnl)} USDT`], ["Paper win rate", pct(stats.win_rate), stats.win_rate >= 0.5 ? "positive" : "negative", `${stats.closed_trades} closed positions`], ["Open positions", stats.open_trades, "", `unrealized ${fmt(stats.unrealized_pnl)} USDT`], ["Max drawdown", `${fmt(stats.max_drawdown)} USDT`, stats.max_drawdown < 0 ? "negative" : "positive", "closed-paper equity" ]].map(([label, value, cls, hint]) => `<div class="metric-card"><span>${label}</span><strong class="${cls}">${value}</strong><small>${hint}</small></div>`).join(""); }
function renderStrategy(strategy) { const conditions = strategy.entry_conditions; $("strategy-meta").textContent = `A candidate needs all three conditions at once. Same-direction signals rearm only after neutral flow.`; $("strategy-flow").innerHTML = [["1. Order book", `Bid/ask depth imbalance must reach <b>+${conditions.long.match(/0\.\d+/)[0]}</b> for LONG or <b>${conditions.short.match(/-0\.\d+/)[0]}</b> for SHORT.`], ["2. Aggressive tape", `Last 3 seconds need at least <b>${conditions.min_trades_3s} trades</b> and buyer/seller delta confirmation.`], ["3. Microprice", "Top-of-book liquidity must point in the same direction as the book and tape."]].map(([title, text]) => `<div class="decision-step"><strong>${title}</strong><span>${text}</span></div>`).join(""); const risk = strategy.risk; const riskText = risk.mode === "fixed_usdt" ? `${fmt(risk.value)} USDT per paper position` : `${(Number(risk.value) * 100).toFixed(2)}% of notional`; $("strategy-result").innerHTML = `<div><b>Candidate is not a trade</b>Arrow and journal row record the setup. <b>Open paper</b> creates the simulated position; stop, target and fee then determine its final PnL.</div><div><b>Current paper boundary</b>${fmt(risk.notional_usdt)} USDT notional · risk ${riskText} · target ${fmt(risk.reward_risk_ratio, 1)}R · expiry ${risk.signal_ttl_seconds}s.</div>`; }
function renderMarket(payload) { const market = Object.values(payload.sources || {}); $("market").innerHTML = market.length ? market.map((item) => { const features = item.features || {}; const buySide = Number(features.imbalance || 0) >= 0; const pressure = buySide ? "BUY-SIDE" : "SELL-SIDE"; const pressurePct = Math.min(100, Math.abs(Number(features.imbalance || 0)) * 100); const href = `/chart.html?source_key=${encodeURIComponent(item.source_key)}&symbol=${encodeURIComponent(item.symbol)}`; return `<article class="market-card"><div class="market-card-head"><div><b>${esc(item.symbol)}</b><span class="market-sub">${esc(sourceLabel(item))} · mid ${fmt(features.mid_price)}</span></div><span class="signal-pill ${buySide ? "long" : "short"}">${pressure}</span></div><div class="pressure"><div class="pressure-label"><span>weighted book imbalance</span><b>${fmt(features.imbalance, 3)}</b></div><div class="pressure-track"><i class="${buySide ? "long" : "short"}" style="width:${pressurePct}%"></i></div></div><div class="feature-grid"><div><span>Spread</span><b>${fmt(features.spread_bps)} bps</b></div><div><span>Tape delta / 3s</span><b class="${Number(features.delta_ratio_3s) >= 0 ? "positive" : "negative"}">${fmt(features.delta_ratio_3s, 3)}</b></div><div><span>Microprice skew</span><b>${fmt(features.microprice_offset_bps)} bps</b></div><div><span>Trades / 3s</span><b>${fmt(features.trades_3s, 0)}</b></div></div><a class="card-link" href="${href}">Inspect this market <span>-&gt;</span></a></article>`; }).join("") : `<div class="empty">Waiting for the first order book snapshot.</div>`; }
function renderAlerts(alerts) { const active = alerts.filter((alert) => !alert.expired && alert.outcome_type === "pending"); $("alerts").innerHTML = alerts.length ? alerts.slice(0, 12).map((alert) => { const actionable = !alert.expired && alert.status === "new" && alert.outcome_type === "pending"; const outcome = alert.outcome_type !== "pending" ? `<span class="outcome ${alert.outcome_type}">${esc(alert.outcome_type.replaceAll("_", " "))}</span>` : alert.expired ? `<span class="outcome expired">expired</span>` : ""; return `<article class="alert-row"><div class="alert-main"><div class="alert-title"><span class="signal-pill ${sideClass(alert.direction)}">${alert.direction} candidate</span><b>${esc(alert.symbol)}</b><span class="score-value">${score(alert.score)}</span><time>${new Date(alert.created_at).toLocaleTimeString()}</time></div><p>${esc(sourceLabel(alert))} · ${esc(alert.reason)}</p><div class="alert-levels"><span>Reference <b>${fmt(alert.reference_price)}</b></span><span>Stop <b>${fmt(alert.stop_loss)}</b></span><span>Target <b>${fmt(alert.take_profit)}</b></span><span>Max risk <b>${fmt(alert.risk_amount, 4)} USDT</b></span></div></div><div class="alert-actions">${outcome}<button class="button tiny" onclick="showAlert(${alert.id})">Evidence</button>${actionable ? `<button class="button tiny primary" onclick="openPaper(${alert.id})">Open paper</button><button class="button tiny ghost" onclick="skipAlert(${alert.id})">Skip</button>` : ""}</div></article>`; }).join("") : `<div class="empty">No candidate satisfies all three order-flow conditions.</div>`; const fresh = new Set(active.map((alert) => alert.id)); if (lastAlertIds.size && [...fresh].some((id) => !lastAlertIds.has(id))) toast("New order-flow candidate", "success"); lastAlertIds = fresh; }
function renderTrades(trades) { const open = trades.filter((trade) => trade.status === "open"); $("trades").innerHTML = open.length ? open.map((trade) => `<div class="position-row"><div><b>${esc(trade.symbol)}</b><span class="market-sub ${sideClass(trade.direction)}">${esc(sourceLabel(trade))} · ${trade.direction} · entry ${fmt(trade.entry_price)}</span></div><div class="position-right"><b class="${(trade.unrealized_pnl || 0) >= 0 ? "positive" : "negative"}">${fmt(trade.unrealized_pnl, 4)} USDT</b><span>${fmt(trade.current_price)} mark</span><button class="icon-action" title="Close paper position" onclick="closeTrade(${trade.id}, ${trade.current_price})">Close</button></div></div>`).join("") : `<div class="empty">No paper position is open.</div>`; }
function renderOutcomeMix(analytics) { const entries = Object.entries(analytics.by_outcome || {}); $("outcome-mix").innerHTML = entries.length ? entries.map(([name, count]) => `<div class="mix-row"><span>${esc(name.replaceAll("_", " "))}</span><b>${count}</b></div>`).join("") : `<div class="empty">Measured outcomes appear after expiry or paper close.</div>`; }
async function showAlert(id) { try { const data = await api(`/api/alerts/${id}`); const alert = data.alert; $("alert-detail").innerHTML = `<p class="eyebrow">CANDIDATE #${alert.id} · ${esc(sourceLabel(alert))}</p><h2><span class="signal-pill ${sideClass(alert.direction)}">${alert.direction}</span> ${esc(alert.symbol)}</h2><p class="dialog-reason">${esc(alert.reason)}</p><div class="detail-grid">${[["Rule strength", score(alert.score)], ["ML win probability", alert.ml_probability == null ? "Filter disabled" : pct(alert.ml_probability)], ["Reference", fmt(alert.reference_price)], ["Stop", fmt(alert.stop_loss)], ["Target", fmt(alert.take_profit)], ["Maximum risk", `${fmt(alert.risk_amount, 4)} USDT`], ["Outcome", alert.outcome_type.replaceAll("_", " ")], ["Source", alert.source_key || "legacy"]].map(([label, value]) => `<div><span>${label}</span><b>${esc(value)}</b></div>`).join("")}</div><h3>Snapshot at candidate time</h3><div class="evidence-grid">${Object.entries(alert.features || {}).map(([key, value]) => `<div><span>${key.replaceAll("_", " ")}</span><b>${fmt(value, Math.abs(Number(value)) < 10 ? 3 : 2)}</b></div>`).join("")}</div>${alert.outcome ? `<h3>Measured outcome</h3><div class="result-box"><b>${esc(alert.outcome.outcome_type.replaceAll("_", " "))}</b><span>mark ${fmt(alert.outcome.price_at_outcome)} · simulated result ${fmt(alert.outcome.hypothetical_pnl, 4)} USDT</span></div>` : ""}<h3>Paper execution</h3>${data.trades.length ? data.trades.map((trade) => `<div class="result-box"><b>${esc(trade.status)}</b><span>${fmt(trade.pnl ?? trade.unrealized_pnl, 4)} USDT · ${esc(trade.exit_reason || "open")}</span></div>`).join("") : `<div class="empty">No paper position was created from this candidate.</div>`}`; $("alert-dialog").showModal(); } catch (error) { toast(error.message, "error"); } }
function closeDialog() { $("alert-dialog").close(); }
async function openPaper(id) { try { await api(`/api/alerts/${id}/paper`, { method: "POST" }); toast("Paper position opened", "success"); await refresh(); } catch (error) { toast(error.message, "error"); await refresh(); } }
async function skipAlert(id) { try { await api(`/api/alerts/${id}/skip`, { method: "POST" }); toast("Candidate marked as skipped"); await refresh(); } catch (error) { toast(error.message, "error"); } }
async function closeTrade(id, price) { if (!price) return toast("Current price is not available", "error"); try { await api(`/api/paper-trades/${id}/close`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ exit_price: price, reason: "manual" }) }); toast("Paper position closed", "success"); await refresh(); } catch (error) { toast(error.message, "error"); } }
async function refresh() { try { const [health, market, alerts, trades, stats, analytics, strategy] = await Promise.all([api("/api/health"), api("/api/market"), api("/api/alerts?limit=50"), api("/api/paper-trades?limit=50"), api("/api/stats/pnl"), api("/api/alerts/analytics"), api("/api/strategy")]); renderSources(health.sources); $("connection-dot").innerHTML = `<i></i> ${health.stream_connected ? "live" : "offline"}`; $("connection-dot").className = `connection ${health.stream_connected ? "online" : "offline"}`; $("last-update").textContent = new Date().toLocaleTimeString(); renderSummary(health, stats, analytics); renderStrategy(strategy); renderMarket(market); renderAlerts(alerts); renderTrades(trades); renderOutcomeMix(analytics); } catch (error) { $("connection-dot").className = "connection offline"; $("connection-dot").innerHTML = "<i></i> API unavailable"; toast(error.message, "error"); } }
bindSourceMenu(); refresh(); setInterval(refresh, 2500);
